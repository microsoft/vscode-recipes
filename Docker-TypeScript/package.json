{
  "name": "app",
  "version": "0.0.0",
  "scripts": {
		"postinstall": "tsc -p ./src",
    "watch": "tsc -w -p ./src",
    "debug": "nodemon --watch ./dist --debug=5858 --nolazy ./dist/index.js",
    "start": "node ./dist/index.js",
    "docker": "docker run -p 3000:3000 app",
    "docker-debug": "docker-compose up"
  },
  "devDependencies": {
    "@types/node": "^6.0.50",
    "typescript": "^2.3.2",
    "nodemon": "^1.11.0"
  },
  "main": "./dist/index.js"
}